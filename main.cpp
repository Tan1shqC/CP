#include <bits/stdc++.h>

#ifdef LOCAL
#include "lib/debug.h"
#else
#define debug(...) 42
#endif

using namespace std;

typedef long long ll;
typedef long double ld;

const ll mod = 1e9 + 7;
const ll N = 1e5 + 5;

struct input
{
    int one;
    int two;
};

vector<int> a;

bool check(int i)
{
    return true;
}

void solve()
{
    int n, m, t, k;
    int i, j;
    int q;

    cin >> n;
}

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    std::cout.tie(NULL);

    int t = 1;
    cin >> t;

    while (t--)
    {
        solve();
    }

    return 0;
}